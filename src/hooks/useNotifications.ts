/**
 * Re-export useNotifications hook from context
 * Provides a convenient import path: import { useNotifications } from '@/hooks/useNotifications'
 */
export { useNotifications } from '@/contexts/NotificationContext';
